<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive files -->
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude sensitive files -->
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
